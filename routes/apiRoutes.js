const express = require('express');
const mongoose = require('mongoose');

const app = express();

module.exports = (
    //Request to get the Books API 
    app.get('/api/books', (req, res) => {
        res.render()
    })

    //Request to post the to Books API 

    //Request to get a specific book based on URL passed in
    


);